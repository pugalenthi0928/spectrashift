# Generated artifacts

Experiment outputs are generated locally and excluded from version control by default. A result may
be promoted into a curated example only after its input provenance, configuration, metrics, and
limitations have been reviewed.

